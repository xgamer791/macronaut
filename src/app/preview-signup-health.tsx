import React from 'react';
import { SignupHealthView } from './signup-health';

/** Temporary ungated copy of the post-create-account Apple Health ask so the
 * screen can be opened without signing up. Remove once that work is done. */
export default function PreviewSignupHealthScreen() {
  return <SignupHealthView />;
}
