/** Re-exports barcode helpers. Prefer `barcodeNormalize` for new call sites. */
export { barcodeVariants, normalizeBarcode } from './barcodeNormalize';
export type { NormalizedBarcode } from './barcodeNormalize';
